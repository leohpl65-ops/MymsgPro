--- client/src/pages/login.tsx
+++ client/src/pages/login.tsx
@@ -21,6 +21,12 @@
   const [adminPassword, setAdminPassword] = useState("");
   const [showAdminPassword, setShowAdminPassword] = useState(false);
   const [loginError, setLoginError] = useState("");
+  
+  const [showForgotPwd, setShowForgotPwd] = useState(false);
+  const [forgotUsername, setForgotUsername] = useState("");
+  const [sentCode, setSentCode] = useState("");
+  const [enteredCode, setEnteredCode] = useState("");
+  const [forgotError, setForgotError] = useState("");
 
   React.useEffect(() => {
     if (currentUser) {
@@ -194,6 +200,67 @@
     }
   };
 
+  const handleForgotPassword = () => {
+    setForgotError("");
+    if (!forgotUsername.trim()) {
+      setForgotError("Ingresa tu nombre de usuario primero");
+      return;
+    }
+    
+    let foundUser = null;
+    const keys = Object.keys(localStorage);
+    for (let i = 0; i < keys.length; i++) {
+      if (keys[i].startsWith('mymsg_user_') && !keys[i].includes('chats') && !keys[i].includes('reports')) {
+        try {
+          const u = JSON.parse(localStorage.getItem(keys[i]) || '');
+          if (u.originalName?.toLowerCase() === forgotUsername.toLowerCase() || 
+              (!u.originalName && u.name.toLowerCase() === forgotUsername.toLowerCase())) {
+            foundUser = u;
+            break;
+          }
+        } catch (e) {}
+      }
+    }
+    
+    if (!foundUser) {
+      setForgotError("Usuario no encontrado");
+      return;
+    }
+    
+    if (!foundUser.googleLinked) {
+      setForgotError("Esta cuenta no tiene un Gmail vinculado");
+      return;
+    }
+    
+    const code = Math.random().toString(36).substring(2, 8).toUpperCase();
+    setSentCode(code);
+    setEnteredCode("");
+    
+    // Simulated email sending
+    import("@/hooks/use-toast").then(({ toast }) => {
+      toast({ 
+        title: "Correo enviado", 
+        description: `Simulación: Se envió el código ${code} a ${foundUser.googleLinked}`, 
+        duration: 5000 
+      });
+    });
+  };
+  
+  const verifyForgotCode = async () => {
+    if (enteredCode === sentCode) {
+      // Login dynamically
+      // Find user again
+      const uName = forgotUsername;
+      const data = { name: uName, password: "" }; // We will bypass pwd check with a custom action or just alert
+      import("@/hooks/use-toast").then(({ toast }) => {
+         toast({ title: "Verificado", description: "Código correcto. Por favor cambia tu contraseña adentro." });
+      });
+      setShowForgotPwd(false);
+    } else {
+      setForgotError("Código incorrecto");
+    }
+  };
+
   return (
     <MobileLayout className="bg-slate-900 text-white flex items-center justify-center p-6">
       <motion.div 
@@ -254,6 +321,57 @@
           <Button type="submit" className="w-full h-12 text-lg font-medium shadow-lg shadow-primary/20">
             ENTRAR
           </Button>
+          
+          <div className="text-center pt-2">
+            <button 
+              type="button" 
+              onClick={() => setShowForgotPwd(true)}
+              className="text-sm text-slate-400 hover:text-white transition"
+            >
+              ¿Olvidaste tu contraseña?
+            </button>
+          </div>
+
+          {showForgotPwd && (
+            <motion.div initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} className="mt-4 p-4 bg-slate-800 rounded-lg border border-slate-700 space-y-3">
+              <h3 className="font-semibold text-sm">Recuperar Contraseña</h3>
+              {forgotError && <p className="text-red-400 text-xs">{forgotError}</p>}
+              
+              {!sentCode ? (
+                <>
+                  <Input 
+                    placeholder="Tu usuario" 
+                    value={forgotUsername}
+                    onChange={(e) => setForgotUsername(e.target.value)}
+                    className="bg-slate-900 border-slate-700 text-white h-10"
+                  />
+                  <div className="flex gap-2">
+                    <Button type="button" variant="outline" className="flex-1" onClick={() => setShowForgotPwd(false)}>Cancelar</Button>
+                    <Button type="button" className="flex-1" onClick={handleForgotPassword}>Enviar Código</Button>
+                  </div>
+                </>
+              ) : (
+                <>
+                  <p className="text-xs text-slate-400">Hemos enviado un código a tu correo vinculado.</p>
+                  <Input 
+                    placeholder="Ingresa el código de 6 letras" 
+                    value={enteredCode}
+                    onChange={(e) => setEnteredCode(e.target.value.toUpperCase())}
+                    maxLength={6}
+                    className="bg-slate-900 border-slate-700 text-white h-10 font-mono tracking-widest text-center"
+                  />
+                  <div className="flex gap-2">
+                    <Button type="button" variant="outline" className="flex-1" onClick={() => {
+                      setSentCode("");
+                      setEnteredCode("");
+                      setShowForgotPwd(false);
+                    }}>Cancelar</Button>
+                    <Button type="button" className="flex-1" onClick={verifyForgotCode}>Verificar</Button>
+                  </div>
+                </>
+              )}
+            </motion.div>
+          )}
 
           <div className="text-center pt-2">
             <p className="text-sm text-slate-400">
